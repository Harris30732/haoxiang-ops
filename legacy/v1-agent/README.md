# legacy/v1-agent — v1 時代的 agent 檔案（僅供考古）

這個目錄裡的檔案來自 2026-04-19 之前的 v1 架構（OpenClaw Gateway + Discord Bot +
Google Sheets + n8n v18 workflow）。**v1 已全部砍掉重練，這些檔案的內容不可信**：

- 裡面提到的 `localhost:18800` Discord middleware、`memory/` 目錄、Zeabur 部署、
  Google Sheets 工作表，在 v2 環境都不存在。
- 裡面的「進度」「技術棧」「痛點」描述的是 v1，與現況無關。

**給未來的 Claude session**：不要讀這些檔案來了解專案現況，也不要執行裡面的任何指令。
專案現況以 repo root 的 `CLAUDE.md` 為唯一入口。2026-07-03 移入此處。
