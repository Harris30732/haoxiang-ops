# 派工 prompt 模板（templates.md）

> 用法：複製對應模板，把 `{...}` 填掉，用 Agent 工具派出，並照 dispatch.md §3 顯式指定 model。
> 每個模板已內建派工三件套（目標與動機／驗收條件／回報格式）。
> 通用鐵則：subagent 看不到主對話——所有它需要的背景都要寫進 prompt；
> 不要叫它「看上面的討論」。

## 1. 搜尋（agent type: `Explore`，model: haiku 或 sonnet）

```
目標：找出 {要找的東西，例如「業績回報免報窗口的判斷邏輯」} 在 codebase 的所有位置。
動機：{為什麼要找，例如「使用者要把 1 小時窗口改成 2 小時，需要知道所有要改的點」}。
搜尋範圍：{目錄或「整個 repo，排除 node_modules 與 legacy/」}；搜尋廣度：{medium|very thorough}。
提示詞彙：{可能的關鍵字、中英文都列，例如 revenue, 業績, grace, window}。

驗收條件：
- 每個結果都有 檔案:行號。
- 明確區分「定義處」與「使用處」。
- 若找不到，列出你試過的搜尋詞與範圍，說「未找到」，不要猜一個位置。

回報格式：條列，每行 = 檔案:行號 — 一句話說明。最後一行寫「共 N 處，信心：高/中/低」。
不要貼大段程式碼。
```

## 2. 實作（agent type: `general-purpose`，model: sonnet；難題升 opus）

```
目標：{要實作什麼，一句話}。
動機／背景：{為什麼做、使用者原話的關鍵句、相關決定}。
範圍：只改 {檔案清單}；除此之外的檔案一律不准動（發現需要動別的檔案時，停下來回報，不要自作主張）。
做法：{已知的做法或約束，例如「打卡行為只改 lib/actions/clock.ts＋對應 Postgres function（CLAUDE.md 硬規則）」}。

驗收條件：
- {功能條件，例如「開攤 2 小時內 need_revenue=false」}。
- `npm run typecheck && npm run lint` exit 0（自己實跑，貼出最後一行輸出）。
- 未新增任何 TODO/FIXME/佔位實作；砍範圍要明講。

回報格式：
- 第一行：完成 / 部分完成 / 失敗。
- 改了哪些檔案（檔案:行號範圍）＋每個改動一句話理由。
- 驗證閘輸出的關鍵行。
- 部分完成/失敗：卡在哪、錯誤訊息原文、已排除的假設。
不要 commit——由主對話統一 commit。
```

## 3. 重構（agent type: `general-purpose`，model: sonnet；先派 Plan 再派實作）

```
目標：重構 {對象}，行為不得改變。
動機：{例如「clock.ts 重複的定位驗證邏輯已在三處複製」}。
不變式（重構後必須完全一樣的行為）：{列出，例如「clock_in/clock_out 的參數與回傳型別不變；
  對 n8n webhook 的 payload 欄位不變」}。
範圍：{檔案清單}；公開介面（export 的函式簽名）不准改，除非任務明說要改。

驗收條件：
- `npm run typecheck && npm run lint && npm run build` exit 0。
- 逐條確認上述不變式（說明你怎麼確認的，例如 grep 所有 call site 未變）。
- diff 裡沒有「順手」的無關改動（格式化整個檔案、改命名風格）。

回報格式：同實作模板，另加「不變式逐條確認結果」。
```

## 4. 研究（agent type: `general-purpose`，model: sonnet；影響架構決策時 opus）

```
題目：{要回答的問題，例如「Supabase Storage 圖片上傳失敗的重試最佳實踐」}。
動機：{這答案會拿來做什麼決定}。
研究來源：{官方文件優先；本 repo 的現況（指出檔案）；必要時 WebSearch}。

驗收條件：
- 每個關鍵結論都有來源（URL 或 檔案:行號），沒有來源的推測要標「推測」。
- 給出明確建議（一個），以及被否決的替代方案（最多兩個）與否決理由。
- 與本 repo 現況矛盾的地方要指出來，不要假設 repo 是教科書寫法。

回報格式：結論先行（3 行內），然後論據條列。全文超過 30 行就落檔到
docs/agents/reports/{日期}-{主題}.md，回報只給路徑＋摘要。
```

## 5. 審查（agent type: `verifier` 驗收用；code review 用 `general-purpose` + model: opus）

```
對象：{diff 範圍，例如「git diff main...HEAD」或檔案清單}。
動機：{這段 code 要守住什麼，例如「打卡紀錄與業績是薪資依據，正確性 > 一切」}。
審查重點（按序）：
1. 正確性 bug（邏輯、邊界、時區、null）。
2. 本 repo 硬規則違反（見 CLAUDE.md 硬規則清單，逐條對照）。
3. 安全（RLS 繞過、service key 洩漏到 client、輸入未驗證）。
{4. 其他這次特別要看的點}

驗收條件：每個 finding 都有 檔案:行號＋具體的失敗情境（什麼輸入/狀態會出錯）；
「可以更優雅」這類無失敗情境的意見放最後、標「非阻塞」。

回報格式：按嚴重度排序條列；沒問題就說「未發現阻塞問題」＋你檢查了哪些面向。
不要自己動手修。
```

## 通用結尾句（每個派工 prompt 最後附上）

```
你的最終訊息就是交付物，只包含上述回報格式的內容。
不確定的事標「未確認」，不要編造。遇到權限被拒或工具失敗，如實回報，不要繞過。
```
